import { getRandomColor } from './randomColor.js';

function randomColorBtn(wrrapper) {
  // создавть кнопку document.createElement()
  // Добавлять ее на страницу wrrapper.append()
  // навешивать обработсчик на клик addEventListener
  // при клике менять цвет на случайный style.backgroundColor = getRandomColor()
}

export { randomColorBtn };
